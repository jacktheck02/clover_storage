{
  "rules": [
    {
      "allowed": {
        "origins": [
          "https://clover-storage.vercel.app",
          "http://localhost:3000",
          "http://127.0.0.1:3000"
        ],
        "methods": ["PUT", "GET", "HEAD"],
        "headers": ["*"]
      },
      "exposeHeaders": ["ETag"],
      "maxAgeSeconds": 3600
    }
  ]
}
