{
  "rules": [
    {
      "allowed": {
        "origins": [
          "https://clover-storage.vercel.app",
          "http://localhost:3000",
          "http://127.0.0.1:3000"
        ],
        "methods": ["GET", "HEAD"],
        "headers": ["range", "if-none-match", "if-modified-since"]
      },
      "exposeHeaders": ["ETag"],
      "maxAgeSeconds": 3600
    }
  ]
}
